echo "Failed to build native extensions for #distance3 method: $1."
echo "If you are using rbenv then install Ruby with -k flag."
echo "If you are using something else then consider contributing."
echo "This is not an error though -- the #distance3 method will fallback to pure Ruby implementation."
